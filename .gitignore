# --- 运行时产物与缓存（任意深度） ---
**/node_modules/
**/.astro/
**/dist/
**/.DS_Store
output/
.playwright-cli/
.playwright-mcp/
viz/
artifacts/

# --- Personal local notes and workflows ---
# The entire docs/ tree is local-only and never enters Git or repository checks.
# No subdirectory is exempt — see agent/adr/0005-public-content-and-private-material-boundary.md.
docs/
.agent-teams/

# --- Personal Claude Code settings (machine-specific permissions) ---
# .claude/skills/ is git-tracked project standard; this file is not.
.claude/settings.local.json
